params = np.linspace(0, 1000, 250)



func(x, y):
    return x + y


def coordinate_ascent(params):
    starting_point = func(params)
    
    for param in params:
        for 







coordinate_ascent([lambda_x, lambda_y])